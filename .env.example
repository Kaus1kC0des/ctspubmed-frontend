# Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# Backend API URL (replace with your backend server URL - no port needed with nginx reverse proxy)
VITE_BACKEND_URL=YOUR_SERVER_IP